# WebShield extension

Manifest V3 extension built with Vite, React, and TypeScript.

## Commands

```bash
npm install
npm run build
npm run test
```

Load `dist/` as an unpacked extension in Chrome after building.
