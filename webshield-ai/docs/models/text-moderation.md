# Text moderation

Text analysis is split into three feature groups:

- toxicity
- hate speech
- profanity

Toxicity and hate speech are model-driven when available. Profanity is implemented as a deterministic detector with rule lists and regex matching for stable precision.
