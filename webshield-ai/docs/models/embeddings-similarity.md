# Embeddings similarity

Preference matching uses sentence embeddings to compare the page title, headings, and selected text content with user interests. The engine returns:

- an overall similarity score
- the best matched interest labels
- a short explanation summary
