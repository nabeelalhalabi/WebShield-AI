# Decision contract

Action values currently supported:

- `allow`
- `warn`
- `blur`
- `hide`
- `replace`
- `block`

The extension applies actions at item scope, while page-level status is represented by the summary status and top-page warning banner.
